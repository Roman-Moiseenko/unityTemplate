using Game.State.Inventory.Common;

namespace Game.State.Inventory.SkillPlans
{
    public class SkillPlan : InventoryItem
    {
        public SkillPlan(InventoryItemData itemData) : base(itemData)
        {
            
        }
    }
}