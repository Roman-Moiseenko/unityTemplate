using Game.State.Entities;

namespace Game.State.Maps.Roads
{
    public class RoadEntity : Entity
    {
        public RoadEntity(EntityData entityData) : base(entityData)
        {
        }
    }
}